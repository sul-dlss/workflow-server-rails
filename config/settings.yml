dor_services:
  url: 'http://localhost:3003'
  token: secret-token

rabbitmq:
  enabled: false
  hostname: localhost
  vhost: /
  username: guest
  password: guest

# checkin keys for honeybadger (actual keys are in shared_configs per environment as needed)
# see https://app.honeybadger.io/projects/58890/check_ins
honeybadger_checkins:
  workflow_monitor: null

redis_url: redis://localhost:6379/
